import { FormattingPipe } from './formatting.pipe';

describe('FormattingPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattingPipe();
    expect(pipe).toBeTruthy();
  });
});
