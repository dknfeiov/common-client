/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ValidMaxLengthDirective } from './valid-max-length.directive';

describe('Directive: ValidMaxLength', () => {
  it('should create an instance', () => {
    const directive = new ValidMaxLengthDirective();
    expect(directive).toBeTruthy();
  });
});