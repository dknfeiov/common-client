/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ValidMinLengthDirective } from './valid-min-length.directive';

describe('Directive: ValidMinLength', () => {
  it('should create an instance', () => {
    const directive = new ValidMinLengthDirective();
    expect(directive).toBeTruthy();
  });
});